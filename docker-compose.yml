version: '2'

services:
  frontend-bolatangkas:
    container_name: frontend-bolatangkas
    image: frontend-bolatangkas:0.0.1
    build: .
    volumes:
    - ./svelte:/app/svelte
    ports:
      - 3014:3014
    restart: always